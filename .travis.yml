sudo: required
services:
  - docker

before_install:
  - docker build -t my/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run my/react-test npm test -- --coverage

after_success:
  - docker build -t my/multi-client ./client
  - docker build -t my/multi-nginx ./nginx
  - docker build -t my/multi-server ./server
  - docker build -t my/multi-worker ./worker
  - docker login -u "$DOCKER_ID" -p "$DOCKER_PASSWORD"
  - docker push my/multi-client
  - docker push my/multi-nginx
  - docker push my/multi-server
  - docker push my/multi-worker
